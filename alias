# ./catgp default uses 4 status dimension all.
# if is needed 2 dimension and complement some, but to complement, 4 is needed.
# so 4th times do predict causes no status dimension, it is predictable by p0.
./catgp $1 | cut -f 3 -d "," | ./catgp $1 | cut -f 3 -d "," | ./catgp $1 | cut -f 3 -d "," | ./catgp $1 | cut -f 3 -d "," | ../p0/p0
